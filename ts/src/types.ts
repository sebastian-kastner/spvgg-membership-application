export type Person = {
    title?: string,
    anrede?: string,
    firstName?: string,
    lastName?: string,
    dateOfBirth?: string,
    city?: string,
    zipCode?: string,
    street?: string,
    streetNumber?: string,
    phoneNumber?: string,
    email?: string,
    isStudent?: boolean,
}
